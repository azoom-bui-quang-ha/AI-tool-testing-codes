import { api } from "../../routes/staffs/get";

// write unit tests for api endpoints routes/staffs/get.js
describe("api", () => {
  // write unit tests for api endpoint
  it("should return a function", () => {
    // write unit test for api endpoint
    expect(typeof api).toBe("function");
  });
});
